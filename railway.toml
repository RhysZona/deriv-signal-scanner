[build]
builder = "nixpacks"
buildCommand = "cd deriv-signal-scanner/client && npm install && npm run build && cd ../server && npm install"

[deploy]
startCommand = "cd deriv-signal-scanner/server && npm start"
